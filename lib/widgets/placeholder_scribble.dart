import 'package:flutter/material.dart';

class PlaceholderScribble extends StatelessWidget {
  const PlaceholderScribble({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 200,
      height: 20,
      color: Colors.grey[400],
    );
  }
}
